<?xml version="1.0" encoding="utf-8"?>

<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:versionCode="1" android:versionName="1.0.0">

  <supports-screens android:smallScreens="true" android:normalScreens="true" android:largeScreens="true" android:anyDensity="true" android:xlargeScreens="true"/>

  <uses-permission android:name="android.permission.RECORD_AUDIO"/>

  <application android:label="@string/app_name" android:icon="@drawable/ic_launcher" android:hardwareAccelerated="false">
    <activity android:name="org.androidaudioplugin.ui.compose.app.PluginManagerActivity" android:hardwareAccelerated="true" android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
    <!--
      ADLplug-AE and OPNplug-AE are hosted by one AudioPluginService, in the same process.
      Each of the plugin libraries has its own JUCE runtime (initialized by aap-juce JuceAppInitializer).
    -->
    <service
        android:name="org.androidaudioplugin.AudioPluginService"
        android:label="@string/app_name"
        android:foregroundServiceType="mediaPlayback"
        android:exported="true">
      <intent-filter>
        <action android:name="org.androidaudioplugin.AudioPluginService.V4"/>
      </intent-filter>
      <meta-data
          android:name="org.androidaudioplugin.AudioPluginService.V4#Plugins"
          android:resource="@xml/aap_metadata"/>
    </service>
    <service android:name="org.androidaudioplugin.AudioPluginViewService" android:exported="true" />
    <!-- a MIDI device for each plugin (see MidiDeviceServices.kt) -->
    <service android:name="org.androidaudioplugin.ports.juce.adlplug_ae.AdlplugMidiDeviceService"
        android:permission="android.permission.BIND_MIDI_DEVICE_SERVICE"
        android:exported="true">
      <intent-filter>
        <action android:name="android.media.midi.MidiDeviceService" />
      </intent-filter>
      <meta-data android:name="android.media.midi.MidiDeviceService"
          android:resource="@xml/midi_device_info" />
    </service>
    <service android:name="org.androidaudioplugin.ports.juce.adlplug_ae.OpnplugMidiDeviceService"
        android:permission="android.permission.BIND_MIDI_DEVICE_SERVICE"
        android:exported="true">
      <intent-filter>
        <action android:name="android.media.midi.MidiDeviceService" />
      </intent-filter>
      <meta-data android:name="android.media.midi.MidiDeviceService"
          android:resource="@xml/midi_device_info_opnplug" />
    </service>
    <service android:name="org.androidaudioplugin.ports.juce.adlplug_ae.AdlplugMidiUmpDeviceService"
        android:permission="android.permission.BIND_MIDI_DEVICE_SERVICE"
        tools:targetApi="35"
        android:exported="true">
      <intent-filter>
        <action android:name="android.media.midi.MidiUmpDeviceService" />
      </intent-filter>
      <property android:name="android.media.midi.MidiUmpDeviceService"
          android:resource="@xml/ump_device_info" />
    </service>
    <service android:name="org.androidaudioplugin.ports.juce.adlplug_ae.OpnplugMidiUmpDeviceService"
        android:permission="android.permission.BIND_MIDI_DEVICE_SERVICE"
        tools:targetApi="35"
        android:exported="true">
      <intent-filter>
        <action android:name="android.media.midi.MidiUmpDeviceService" />
      </intent-filter>
      <property android:name="android.media.midi.MidiUmpDeviceService"
          android:resource="@xml/ump_device_info_opnplug" />
    </service>

    <provider android:name="androidx.startup.InitializationProvider"
        android:authorities="${applicationId}.androidx-startup"
        android:exported="false"
        tools:replace="android:authorities"
        tools:node="merge">
        <meta-data android:name="org.androidaudioplugin.juce.JuceAppInitializer" android:value="androidx.startup" />
    </provider>
  </application>
</manifest>
